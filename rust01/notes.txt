    1.options <T> 

     if we need to get the value out of a option we use unwrap() method. 

    2. the vec::pop() always return a option becasue a vec can be empty      
    
    let mut v = vec![10, 20, 30];
    let x = v.pop();